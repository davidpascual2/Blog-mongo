const Post = require('./Post'); //referenced in controllers/postController
const User = require('./User'); //referenced in controllers/commentController
const Category = require('./Category');

module.exports = { Post, User, Category };